[package]
name = "dump_legacy"
version = "0.1.0"
edition = "2024"
publish = false

[dependencies]
macos-unifiedlogs = { path = "../..", default-features = false, features = ["legacy"] }
serde = { version = "1.0.228", features = ["derive"] }
serde_json = "1.0.149"

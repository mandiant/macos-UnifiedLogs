# logrs 

A simple example binary that uses the `macos-unifiedlogs` library to parse Apple Unified Logs. It currently has a few goals:

1. Primary goal is to help troubleshoot Unified Log any format changes
2. Provide similar cli as `log` command. log + Rust (rs) = `logrs`
3. Leverage the new rewrite capabilities 